// status — Publish and update Rust crates on crates.io + academic releases on Zenodo
// usage: status [args...]

const args = process.argv.slice(2);
console.log(`status: ${args.join(" ") || "(no args)"}`);
console.log("TODO: implement status skill");

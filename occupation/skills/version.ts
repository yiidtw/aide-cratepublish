// version — Publish and update Rust crates on crates.io + academic releases on Zenodo
// usage: version [args...]

const args = process.argv.slice(2);
console.log(`version: ${args.join(" ") || "(no args)"}`);
console.log("TODO: implement version skill");
